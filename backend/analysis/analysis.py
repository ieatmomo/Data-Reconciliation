def analyse_files(fileOld, fileNew):
    """
    Custom function to process the uploaded files.
    This is a placeholder for the actual analysis logic.
    """
    # For now, just return a simple message
    return f"Files {fileOld.filename} and {fileNew.filename} processed successfully."

def create_trend_graph(query_results):
    ''' MUST ENCODE AS BASE64 STRING '''
    return 0